# magview — operator's manual

Marine Magnetics Explorer acquisition and display, version 2026.08.20.

This file is generated from the help built into the program:
`magview --help-doc > docs/HELP.md`, or `make help-doc`. Edit
`src/mv_help.c` and regenerate — do not edit this file.

## Contents

1. What this program does
2. Connecting to the instrument
3. The status strip
4. GPS source
5. Field — the scrolling strips
6. Chart — the plotter
7. Log
8. Console
9. Keyboard and mouse
10. When something is wrong
11. Command line and environment

---

## What this program does

magview reads a Marine Magnetics Explorer over its serial link, shows the total field on a scrolling strip, plots the survey track on a chart, and logs every reading to a georeferenced CSV. It is a straightforward replacement for the vendor's acquisition software.

Everything works offline. There is no basemap imagery, no tile server and no internet dependency anywhere, because a survey vessel usually has none of them.

> **Note.** This is acquisition and display only. It does not configure the instrument's internal settings or command the tow; it reads what the Explorer sends and records it.

---

## Connecting to the instrument

The Explorer's serial cable appears to the computer as a serial port. Press Connect... on the rail, choose the port, set the baud rate (9600, 8N1 is the Explorer's default) and connect. If the port you want is not listed, plug the cable in and press Rescan.

| | |
|---|---|
| **Not connected** | No port is open. Nothing is being read. |
| **Open, no data** | The port is open but no mag line has arrived yet. Check the baud rate and that the instrument is powered and sampling. |
| **Receiving** | Mag data is arriving and being parsed. |
| **Link error** | The port was lost — unplugged, or another program took it. Reconnect. |

Disconnect closes the port; the instrument carries on regardless. Clear empties the buffered readings and the track without touching the log.

---

## The status strip

The strip across the top is the same on every page. Read it left to right.

| | |
|---|---|
| **Explorer** | The instrument, or "Explorer (sim)" when running against the simulator. |
| **Link state** | See Connecting. |
| **Field** | The latest total field, in nanoTesla. |
| **Signal** | Signal strength. It turns amber below 800 (weak) and green above 1300 (excellent). |
| **Depth** | Towfish depth, when the instrument's output carries it. |
| **Position** | Latitude and longitude of the latest fix, or "no GPS fix". Amber when the fix has gone stale. |
| **Rate** | Measured sample rate, in hertz. |
| **Logging** | A red REC marker while a log is being written. |

---

## GPS source

Every reading is tagged with the latest GPS fix, which is what georeferences the log and draws the plotter track. Rail, GPS source... chooses where that fix comes from, and the choice is remembered between sessions.

| | |
|---|---|
| **Interleaved on the mag line** | The Explorer is set to pass NMEA GPS through its own serial output, mixed in with the mag data. Nothing else to configure. |
| **A separate serial port** | The GPS is its own device on a second port. Choose the port and its baud rate — 4800 is the classic NMEA GPS baud. |
| **UDP network (NMEA over IP)** | The position comes over the network as NMEA, e.g. from a navigation PC. Set the port to listen on; 10110 is the conventional one. |

A fix seen on the mag line is always used, whichever source is selected — if a rig sends it there, it counts. The dedicated sources are for rigs that do not.

---

## Field — the scrolling strips

Field is the main acquisition display: the total field on a chart-recorder strip, newest at the right, scrolling away to the left.

| | |
|---|---|
| **Window** | How many seconds of history are shown at once. |
| **Depth strip** | Add a towfish-depth strip below the field, on the same time axis, drawn with deeper lower down. |
| **Signal strip** | Add a signal-strength strip, to watch the quality of the readings. |

> **Note.** The field strip auto-ranges to what is on screen. This is the whole point: the anomalies a survey looks for are a few tens of nT riding on a background of around 50,000 nT, and a fixed 0 to 70,000 axis would hide them completely. The axis figures on the left tell you the range in force.

Rest the pointer anywhere on a strip for the value at that moment.

---

## Chart — the plotter

Chart is a plan view of the survey track and where the towfish is now. There is deliberately no basemap: a graticule, a scale bar and a north arrow work at sea with no data connection.

| | |
|---|---|
| **Fit** | Set the view to contain the whole track. |
| **+ and -** | Zoom about the middle of the plot. |
| **Colour by field** | Post each fix by how far its reading sits from the survey mean — cool below, warm above — so a target shows as a hot patch on the line as you pass over it. |
| **Follow** | Keep the live position centred as the boat moves. |

### With the mouse

| | |
|---|---|
| **Drag** | Pan. |
| **Wheel** | Zoom about the pointer. |

The side panel gives the current position, the number of track points, the live field, and — when the pointer is over the plot — the position and field under it.

> **Note.** The track is drawn only from readings that carried a position. With no GPS fix there is no track, however much mag data is arriving; set the GPS source first.

---

## Log

Log writes every reading to a CSV as it arrives. This is the survey record.

| | |
|---|---|
| **Folder** | Where the file is written. It defaults to your Documents folder. |
| **Also keep the raw serial** | Alongside the CSV, write every serial line verbatim to a .raw file — the unarguable original if a question ever comes up. |
| **Start / Stop logging** | Open or close the log file. |

### What a row holds

Columns: utc, tick_s, field_nT, signal, depth_m, altitude_m, quality, lat, lon. One row per reading — the magnetic data and the position it was taken at, so the file is georeferenced mag data ready for a survey package.

> **Note.** A field the instrument's output did not carry is left blank rather than logged as a misleading zero. A reading taken before any GPS fix has an empty position; set the GPS source so every row is placed.

The file is flushed after every row, so a power interruption costs at most the line in flight. The Latest readings table shows the tail of what is being written.

---

## Console

Console shows every line the instrument sent, verbatim, following the tail. It is the first place to look when something behaves oddly, and the first thing to quote in a fault report.

| | |
|---|---|
| **Unparsed** | Lines that matched no known format. A steadily rising count means the wrong baud rate, or an output format magview does not yet read. |
| **Bad checksum** | Lines whose NMEA checksum did not verify. A handful is normal; a climbing count points at a noisy link. |

A line that fails its checksum is still parsed — losing a fix costs a position, and every field is range-checked anyway.

---

## Keyboard and mouse

| | |
|---|---|
| **Hover** | Rest the pointer on any control for a one-line description of what it does. |
| **F1** | Open this page. |
| **Escape** | Cancel the dialog that is open. |
| **Mouse wheel** | Zoom the chart, or scroll a list. |
| **Drag** | Pan the chart. |

A dialog's title-bar close button means Cancel, the same as Escape.

---

## When something is wrong

| | |
|---|---|
| **No serial ports listed** | Plug the cable in and press Rescan. On Linux the user must be in the dialout group to open a serial port. |
| **Open, but no data** | Wrong baud rate, or the instrument is not sampling. 9600 8N1 is the Explorer default; try the others from the Connect dialog. |
| **Unparsed count climbing** | The output format is one magview does not read, or the baud is wrong. Look at the Console to see what is actually arriving. |
| **No track on the chart** | The readings have no position. Set the GPS source, and check the status strip shows a fix. |
| **Field trace looks flat** | It is auto-ranged; read the axis figures. A genuinely flat trace over a small range is still drawn across the strip. |
| **Text too small or too large** | The interface scales itself to the display. Override it with the MAGVIEW_SCALE environment variable. |

---

## Command line and environment

| | |
|---|---|
| **--sim** | Run against a simulated Explorer that speaks the real wire format. Everything in the program works with no hardware attached. |
| **--port DEV** | Open this serial port at startup. |
| **--baud N** | Baud rate for --port (default 9600). |
| **--help** | Command-line summary. |
| **--help-doc** | Write this manual to standard output as Markdown. |
| **MAGVIEW_SCALE** | Override the interface scale, e.g. 2 on a HiDPI display. |

